<s:SurfaceWindow x:Class="Team_Roasters.Home_Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:s="http://schemas.microsoft.com/surface/2008"
    Title="Team_Roasters" mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="1080" d:DesignWidth="5000" SizeToContent="WidthAndHeight">
	<s:SurfaceWindow.Resources>
<!-- Base Grid style for 55 dpi -->
		<Style TargetType="{x:Type Control}" x:Key="ControlBaseStyle">
			<Setter Property="FocusVisualStyle"	
				Value="{x:Null}"/>
			<Setter Property="SnapsToDevicePixels"
				Value="False"/>
			<Setter Property="FontFamily"
				Value="Segoe360"/>
			<Setter Property="FontWeight"
				Value="Normal"/>
			<Setter Property="FontSize"
				Value="17"/>
			<Setter Property="Padding"
				Value="6,2,10,10"/>
			<Setter Property="MinHeight"
				Value="38"/>
			<Setter Property="MinWidth"
				Value="38"/>
			<Setter Property="Margin"
				Value="1"/>
			<Setter Property="HorizontalContentAlignment"
				Value="Left"/>
			<Setter Property="VerticalContentAlignment"
				Value="Top"/>
			<Setter Property="BorderThickness"
				Value="2"/>
		</Style>

<!-- ButtonBase -->
		<Style x:Key="HomeButton"
			TargetType="{x:Type s:SurfaceButton}" 
			BasedOn="{StaticResource ControlBaseStyle}">
			<Setter Property="IsTabStop"
				Value="False" />
			<Setter Property="Focusable"
				Value="True" />
			<Setter Property="Background"
				Value="{DynamicResource {x:Static s:SurfaceColors.ButtonBackgroundBrushKey}}" />
			<Setter Property="Foreground"
				Value="{DynamicResource {x:Static s:SurfaceColors.ButtonForegroundBrushKey}}" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="{x:Type s:SurfaceButton}">
						<ControlTemplate.Resources>
							<Storyboard x:Key="Press">
								<DoubleAnimationUsingKeyFrames BeginTime="00:00:00"
									Storyboard.TargetName="PressOverlay"
									Storyboard.TargetProperty="(UIElement.Opacity)">
									<SplineDoubleKeyFrame KeyTime="00:00:00.1000000"
										Value="1" />
								</DoubleAnimationUsingKeyFrames>
							</Storyboard>
							<Storyboard x:Key="Release">
								<DoubleAnimationUsingKeyFrames BeginTime="00:00:00"
									Storyboard.TargetName="PressOverlay"
									Storyboard.TargetProperty="(UIElement.Opacity)">
									<SplineDoubleKeyFrame KeyTime="00:00:00.1500000"
										Value="0" />
								</DoubleAnimationUsingKeyFrames>
							</Storyboard>
						</ControlTemplate.Resources>
						<Grid x:Name="Grid"
							SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
							<Rectangle  x:Name="ButtonBody"
								Fill ="{TemplateBinding Background}"
								SnapsToDevicePixels ="{TemplateBinding SnapsToDevicePixels}"/>
							<Rectangle  x:Name="PressOverlay"
								Opacity="0"
								Fill ="{DynamicResource {x:Static s:SurfaceColors.ButtonBackgroundPressedBrushKey}}" 							             
								SnapsToDevicePixels ="{TemplateBinding SnapsToDevicePixels}"/>
							<ContentPresenter  x:Name="Content"
								Margin="{TemplateBinding Padding}"
								SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
						</Grid>

						<ControlTemplate.Triggers>
							<Trigger Property="IsPressed"
								Value="True">
								<Trigger.EnterActions>
									<BeginStoryboard Storyboard="{StaticResource Press}" />
								</Trigger.EnterActions>
								<Trigger.ExitActions>
									<BeginStoryboard Storyboard="{StaticResource Release}" />
								</Trigger.ExitActions>
								<Setter Property="Foreground"
									Value="{DynamicResource {x:Static s:SurfaceColors.ButtonForegroundPressedBrushKey}}"/>
							</Trigger>
							<Trigger Property="IsEnabled"
								Value="False">
								<Setter Property="Fill"
									Value="{DynamicResource {x:Static s:SurfaceColors.ButtonBackgroundDisabledBrushKey}}"
									TargetName="ButtonBody" />
								<Setter Property="Foreground"
									Value="{DynamicResource {x:Static s:SurfaceColors.ButtonForegroundDisabledBrushKey}}"/>
								<Setter Property="Opacity"
									Value="0.33"
									TargetName="Content" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

	</s:SurfaceWindow.Resources>
    <Grid Background="White" Width="1920" Height="1080">
        <Grid.RowDefinitions>
            <RowDefinition Height="217*" />
            <RowDefinition Height="863"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="640"/>
            <ColumnDefinition Width="700"/>
            <ColumnDefinition Width="580*" />
        </Grid.ColumnDefinitions>
        <Grid Height="220" Margin="0,0,0,0" Grid.ColumnSpan="3" ShowGridLines="True">
            <s:SurfaceButton Name="ccf_hidden_text" Visibility="Collapsed" Content="Image" Background="White" FontSize="20" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Click="ccf_hidden_text_Click" />
            <Grid Name="ccf_grid" Margin="0,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="110*" />
                    <RowDefinition Height="110*" />
                </Grid.RowDefinitions>
                <Image Stretch="Uniform" Source="Resources/Fitout-S-child-cancer.jpg" Margin="100,10,10,10" HorizontalAlignment="Left" Width="331" Grid.RowSpan="2" />
            </Grid>
        </Grid>
        
        <s:SurfaceButton Grid.Row="1" 
                         Grid.Column="0" 
                         Grid.RowSpan="1" 
                         Grid.ColumnSpan="1" 
                         Name="what_we_do" 
                         Content="What We Do" 
                         Background="#FF6F6FFF" Margin="52,52.5,46.5,0" FontSize="48" Height="252.5" VerticalAlignment="Top" d:LayoutOverrides="Height" Style="{DynamicResource HomeButton}"/>
        
        <s:SurfaceButton Grid.Row="1" 
                         Grid.RowSpan="1" 
                         Grid.ColumnSpan="1" 
                         Name="how_you_can_help" 
                         Content="How you can help" 
                         Background="#FF6F6FFF" FontSize="48" Margin="0,52.5,150.5,0" Height="252.5" VerticalAlignment="Top" Grid.Column="1" Style="{DynamicResource HomeButton}"/>
        
        <s:SurfaceButton Grid.Row="1" 
                         Grid.RowSpan="1" 
                         Grid.ColumnSpan="1" 
                         Name="family_and_support" 
                         Content="Family and Support"
                         Background="#FF6F6FFF" Margin="52,368.422,46.5,242.078" FontSize="48" Style="{DynamicResource HomeButton}"/>
        
        <s:SurfaceButton Grid.Row="1" 
                         Grid.RowSpan="1"
                         Grid.ColumnSpan="1"
                         Name="volunteers_and_corporate"
                         Content="Volunteers and &#xd;&#xa;corporate sponsors"
                         Background="#FF6F6FFF" Margin="0,368.422,158.5,242.078" FontSize="48" Grid.Column="1" Style="{DynamicResource HomeButton}"/>
    </Grid>    
</s:SurfaceWindow>